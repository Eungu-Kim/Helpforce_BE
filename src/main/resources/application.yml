# ===============================
# = H2 Database Configuration (?? ????)
# ===============================
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  # H2 Console ??? (http://localhost:8080/h2-console ?? ??)
  h2:
    console:
      enabled: true

  # ===============================
  # = JPA / Hibernate Configuration
  # ===============================
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# ===============================
# = MySQL Database Configuration (?? DB ???? ??)
# ===============================
# spring:
#   datasource:
#     driver-class-name: com.mysql.cj.jdbc.Driver
#     url: jdbc:mysql://localhost:3306/web?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
#     username: root
#     password:

# ===============================
# = JWT Configuration
# ===============================
jwt:
  secret: your-super-secret-key-change-this-in-production-must-be-very-long-at-least-256-bits
  access-token-validity: 3600000
  refresh-token-validity: 604800000